# Copy this file to a new file named .env and paste your own key after the = sign.
# The real .env file is ignored by git, so your key is never uploaded.
# Never put a real key in this example file.
ANTHROPIC_API_KEY=your-key-here
